using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//
// 터렛의 총구. 발사 지점으로 사용되는 오브젝트에 들어갈 스크립트.
// 자식 객체 검색 편의성 용도로 제작.
//

public class Muzzle : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Fire()
    {

    }
}
